<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <template id="contact_page" name="Contact">
    <t t-call="website.layout">

      <!-- HERO -->
      <section class="contact-hero">
        <div class="contact-hero-overlay">
          <div class="container text-center">
            <h1>Nous sommes là pour vous 💬</h1>
            <p class="contact-hero-sub"> Une question, un conseil, une commande ?<br /> Notre équipe
              vous répond sous 24h. </p>
          </div>
        </div>
      </section>

      <!-- CONTENU PRINCIPAL -->
      <section class="contact-main container">
        <div class="row g-5">

          <!-- GAUCHE : INFOS -->
          <div class="col-lg-5 col-md-5">
            <h2>Parlons sommeil !</h2>
            <p class="contact-intro">
              Notre équipe est là pour vous accompagner
              dans le choix de votre matelas.
            </p>
            <div class="contact-info-card">

              <div class="contact-info-item">
                <div class="contact-info-icon">
                  <i class="bi bi-geo-alt-fill" />
                </div>
                <div>
                  <p class="contact-info-label">Adresse</p>
                  <a href="https://maps.google.com" target="_blank">
                    123 Rue du Sommeil, Paris
                  </a>
                </div>
              </div>

              <div class="contact-info-item">
                <div class="contact-info-icon">
                  <i class="bi bi-telephone-fill" />
                </div>
                <div>
                  <p class="contact-info-label">Téléphone</p>
                  <a href="tel:+33600000000">+33 6 00 00 00 00</a>
                </div>
              </div>

              <div class="contact-info-item">
                <div class="contact-info-icon">
                  <i class="bi bi-envelope-fill" />
                </div>
                <div>
                  <p class="contact-info-label">Email</p>
                  <a href="mailto:contact@matelas.com">
                    contact@matelas.com
                  </a>
                </div>
              </div>

              <div class="contact-info-item">
                <div class="contact-info-icon">
                  <i class="bi bi-clock-fill" />
                </div>
                <div>
                  <p class="contact-info-label">Horaires</p>
                  <p class="contact-info-value"> Lun – Ven : 9h – 18h<br />Sam : 10h – 16h </p>
                </div>
              </div>

            </div>
          </div>

          <!-- DROITE : FORMULAIRE -->
          <div class="col-lg-6 col-md-7">
            <div class="contact-form-box">
              <h2>Envoyez-nous un message</h2>

              <div id="contact-success" class="contact-success" style="display:none">
                <i class="bi bi-check-circle-fill" />

                <h3>Merci pour votre message !</h3>

                <p>
                  Notre équipe vous répondra dans les plus brefs délais.
                </p>

              </div>

              <div class="mb-3">
                <label class="contact-label">Nom *</label>
                <input type="text" id="c-nom" class="contact-input"
                  placeholder="Votre nom" />
              </div>

              <div class="mb-3">
                <label class="contact-label">Prénom *</label>
                <input type="text" id="c-prenom" class="contact-input"
                  placeholder="Votre prénom" />
              </div>

              <div class="mb-3">
                <label class="contact-label">Email *</label>
                <input type="email" id="c-email" class="contact-input"
                  placeholder="votre@email.com" />
              </div>

              <div class="mb-3">
                <label class="contact-label">Téléphone</label>
                <input type="tel" id="c-tel" class="contact-input"
                  placeholder="+33 6 00 00 00 00" />
              </div>

              <div class="mb-3">
                <label class="contact-label">Sujet</label>
                <select id="c-sujet" class="contact-input">
                  <option value="">Choisir un sujet...</option>
                  <option>Question sur un produit</option>
                  <option>Suivi de commande</option>
                  <option>Retour / Remboursement</option>
                  <option>Conseil personnalisé</option>
                  <option>Autre</option>
                </select>
              </div>

              <div class="mb-3">
                <label class="contact-label">Message *</label>
                <textarea id="c-message" class="contact-input contact-textarea"
                  placeholder="Votre message..." />
              </div>

              <button class="btn-contact-send" onclick="envoyerContact()">
                <i class="bi bi-send-fill me-2" /> Envoyer mon message </button>

            </div>
          </div>

        </div>
      </section>

      <!-- Newsletter -->
      <section class="newsletter-section">
        <div class="container">
          <div class="newsletter-box">
            <div class="newsletter-text">
              <p class="section-label"><i class="bi bi-envelope-fill"></i> RESTEZ INFORMÉ</p>
              <h2>Ne ratez aucune offre exclusive !</h2>
              <p>Inscrivez-vous et recevez nos meilleures offres directement dans votre boîte mail.</p>
            </div>
            <div class="newsletter-form">
              <input type="email" class="newsletter-input" placeholder="Votre adresse email..." />
              <a href="#" class="btn btn-newsletter">S'inscrire →</a>
            </div>
          </div>
        </div>
      </section>

    </t>
  </template>

</odoo>