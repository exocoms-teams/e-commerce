<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_daily_trend_scoring" model="ir.cron">
        <field name="name">Produits Tendance : scoring quotidien</field>
        <field name="model_id" ref="model_trend_scoring_orchestrator"/>
        <field name="state">code</field>
        <field name="code">model.run_daily_scoring()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="True"/>
    </record>
</odoo>