<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================
         WIN-45 : Landing Page SaaS - route "/"
         Hérite de la homepage Odoo standard.
         Structure "oe_structure" éditable via le constructeur de
         site natif (blocs Bootstrap 5) : aucun CSS custom ici.
    ============================================================ -->

    <template id="homepage" name="Landing Page - Détection Produits Tendance"
          inherit_id="website.homepage" priority="99">

        <xpath expr="//div[hasclass('oe_structure')]" position="replace">
            <div class="oe_structure" id="oe_structure_produits_tendance_homepage">

                <!-- Section de départ : bloc "Cover" natif Odoo.
                     Editable/remplaçable ensuite depuis le constructeur
                     de site (drag & drop d'autres blocs). -->
                <section class="s_cover parallax s_parallax_is_fixed pt96 pb96 o_colored_level"
                         data-scroll-background-ratio="0" data-snippet="s_cover">
                    <div class="o_we_bg_filter bg-black-50"/>
                    <div class="container">
                        <div class="row s_nb_column_fixed">
                            <div class="col-lg-12 pt16 pb16">
                                <h1 class="text-white">
                                    Détectez les produits tendance avant vos concurrents
                                </h1>
                                <p class="lead text-white">
                                    Notre plateforme analyse en continu les signaux e-commerce
                                    (ventes, publicités, réseaux sociaux) pour repérer les
                                    produits gagnants avant tout le monde.
                                </p>
                                <a href="/shop" class="btn btn-primary">Découvrir la plateforme</a>
                            </div>
                        </div>
                    </div>
                </section>

                <!-- WIN-104 : bandeau d'indicateurs clés, juste sous le hero.
                     Chiffres calculés ici (seul endroit de ce template ayant
                     accès à `request`) puis passés au sous-template
                     paramétré template_key_indicators - même principe que
                     template_how_it_works : le sous-template lui-même ne
                     dépend d'aucun contexte HTTP, donc testable directement. -->
                <t t-set="win104_total_products"
                   t-value="request.env['trend.product'].sudo().search_count([])"/>
                <t t-set="win104_scores"
                   t-value="request.env['trend.product'].sudo().search([]).mapped('current_score')"/>
                <t t-set="win104_avg_score"
                   t-value="(sum(win104_scores) / len(win104_scores)) if win104_scores else 0.0"/>
                <t t-call="produits_tendance.template_key_indicators">
                    <t t-set="total_products" t-value="win104_total_products"/>
                    <t t-set="avg_score" t-value="win104_avg_score"/>
                </t>

                <!-- WIN-103 : "Comment ça marche" - 3 étapes juste sous le
                     hero. Sous-template dédié (template_how_it_works,
                     ci-dessous) : pas de dépendance à website.layout/request,
                     donc testable directement en TransactionCase, même
                     principe que template_product_cards pour le dashboard. -->
                <t t-call="produits_tendance.template_how_it_works"/>

            </div>
        </xpath>

    </template>

    <!-- WIN-104 : sous-template paramétré (total_products, avg_score fournis
         par l'appelant) - aucun accès à `request`, testable directement en
         lui passant des valeurs explicites, même principe que
         template_product_cards. Le 3e indicateur (canaux de collecte) est
         un fait d'architecture verifiable (les 3 valeurs du champ
         trend.product.source : api/scraping/crowdsourcing), pas une
         statistique de croissance inventée - contrainte explicite du
         ticket ("jamais un chiffre qui ne reflète pas la réalité"). Pas de
         4e indicateur "fréquence de mise à jour du classement" (suggéré en
         exemple par le ticket) : aucun cron ne recalcule les scores
         aujourd'hui, l'afficher serait un chiffre faux. -->
    <template id="template_key_indicators" name="Bandeau d'indicateurs clés">
        <section class="o_winners_key_indicators py-4">
            <div class="container">
                <div class="o_winners_stat_tiles row g-3">
                    <div class="col-md-4">
                        <div class="o_winners_stat_tile">
                            <span class="o_winners_stat_tile__label">Produits suivis</span>
                            <span class="o_winners_stat_tile__value"><t t-esc="total_products"/></span>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="o_winners_stat_tile">
                            <span class="o_winners_stat_tile__label">Score de tendance moyen</span>
                            <span class="o_winners_stat_tile__value"><t t-esc="'%.1f' % avg_score"/><small>/100</small></span>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="o_winners_stat_tile">
                            <span class="o_winners_stat_tile__label">Canaux de collecte</span>
                            <span class="o_winners_stat_tile__value">3</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </template>

    <!-- Sous-template autonome (aucun t-call="website.layout", aucun accès
         à `request`) : uniquement du QWeb statique, pour rester testable
         hors contexte HTTP. -->
    <template id="template_how_it_works" name="Comment ça marche - 3 étapes">
        <section class="o_winners_how_it_works py-5">
            <div class="container">
                <h2 class="o_winners_how_it_works__title text-center">Comment ça marche</h2>
                <div class="row mt-4">
                    <div class="col-md-4 mb-4">
                        <div class="o_winners_how_it_works__step">
                            <i class="fa fa-search o_winners_how_it_works__icon"/>
                            <h3 class="o_winners_how_it_works__step_title">1. On collecte les données</h3>
                            <p class="o_winners_how_it_works__step_text">
                                Scraping, API officielles et signalements de la communauté, en continu.
                            </p>
                        </div>
                    </div>
                    <div class="col-md-4 mb-4">
                        <div class="o_winners_how_it_works__step">
                            <i class="fa fa-line-chart o_winners_how_it_works__icon"/>
                            <h3 class="o_winners_how_it_works__step_title">2. On calcule un score de tendance</h3>
                            <p class="o_winners_how_it_works__step_text">
                                Chaque produit est noté selon ses ventes, ses publicités et son engagement social.
                            </p>
                        </div>
                    </div>
                    <div class="col-md-4 mb-4">
                        <div class="o_winners_how_it_works__step">
                            <i class="fa fa-trophy o_winners_how_it_works__icon"/>
                            <h3 class="o_winners_how_it_works__step_title">3. Vous découvrez les produits gagnants</h3>
                            <p class="o_winners_how_it_works__step_text">
                                Accédez au classement avant que la concurrence ne s'en aperçoive.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </template>

</odoo>